import React from 'react';

const Loading = () => {
    return (
        <div>
            <h4 style={{textAlign:'center'}}> Loading ...</h4>
            
        </div>
    );
};

export default Loading;